rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write in test mode (change before going to production)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
